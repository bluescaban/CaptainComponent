# Captain Component — Environment Variables
# Copy this file to .env and fill in your values.
# NOTE: The OpenAI API key is entered directly in the plugin UI at runtime.
# It is stored in Figma's clientStorage (per-user, encrypted).

OPENAI_API_KEY=sk-...
